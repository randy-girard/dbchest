<% content_for :title, "Editing credential" %>

<h1>Editing credential</h1>

<%= render "form", credential: @credential %>

<br>

<div>
  <%= link_to "Show this credential", [@cluster, @node, @credential] %> |
  <%= link_to "Back to credentials", cluster_path %>
</div>
